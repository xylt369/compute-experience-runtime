{
  "name": "compute-experience-runtime",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/renderers",
    "packages/ui"
  ],
  "bin": {
    "cx": "./bin/cx.ts"
  },
  "scripts": {
    "dev": "vite",
    "cx": "vitest --run bin/cx.ts",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "ajv": "^8.17.1",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
