# AI Model Compiler — inject API keys via environment (never commit secrets).
COMPILE_LLM_API_KEY=
COMPILE_LLM_BASE_URL=https://api.openai.com/v1
COMPILE_LLM_MODEL=gpt-4o-mini

# Playground (Vite) — browser-exposed compile adapter (optional; mock used when unset).
VITE_COMPILE_LLM_API_KEY=
VITE_COMPILE_LLM_BASE_URL=https://api.openai.com/v1
VITE_COMPILE_LLM_MODEL=gpt-4o-mini
